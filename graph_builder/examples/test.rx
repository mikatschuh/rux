let main usize = { // place holder for a function (wip)
    var last_last = 0
    var last = 1

    var i = 0
    loop {
        let current = last + last_last
        last_last = last
        last = current
        i++

        if i > 255 break
    }
    last
}
