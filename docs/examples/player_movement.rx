FRICTION = 30
const FRICTION = 30

Camera = struct {
    vel   [2]Vel
    pos   Vec2
}

struct Camera {
    vel   [2]Vel
    pos   Vec2
}

fn tick(cam -> Camera, friction f32, acc [2]Acc, delta_time Time) {
    cam.vel += acc * delta_time;

    let print_hello = fn() void println("Hello")

    cam.vel *= exp(EULER, -friction * delta_time)

    cam.pos += vel * delta_time
}

main = fn() void {
    println("Hello")
}

fn main() void {
    println("Hello")
}
